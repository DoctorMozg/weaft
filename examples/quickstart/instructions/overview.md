---
name: overview
description: Project-wide guidance compiled into the host's instruction file.
---
# {{ project.name }} — agent guidance

You are working under **{{ host.display_name }}**. Prefer the project's existing
conventions, keep changes scoped, and ask before any destructive operation.

This file is the Instruction kind: it is Native on Claude Code (`CLAUDE.md`) and on the
generic AGENTS.md host (merged into `AGENTS.md`), and it is dropped on Cursor, which has
no instruction surface.
